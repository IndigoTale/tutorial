# Gitコマンド備忘録
## 初期化
```
git init
git add README.md //README.mdを編集した後
git commit -m "add README.md"
git remote add origin https://github.com/(yourAccount)/yourRepogitory).git
git push -u origin master
```
